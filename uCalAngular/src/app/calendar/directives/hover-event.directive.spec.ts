import { HoverEventDirective } from './hover-event.directive';

describe('HoverEventDirective', () => {
  it('should create an instance', () => {
    // const directive = new HoverEventDirective();
    // expect(directive).toBeTruthy();
  });
});
